#!/bin/sh
set -e
APT=/usr/sbin/apt
APTGET=/usr/sbin/apt-get
APTOVERLAY=/usr/sbin/apt-overlay
case $1 in

	configure)
		for i in $APT $APTGET $APTOVERLAY
		do
			dpkg-divert --package lliurex-apt-divert --add --rename --divert ${i}.neon ${i}
		done
		;;

esac

#DEBHELPER#
